[docker_server]
Product-Docker-Init ansible_host=xxx.xxx.xxx.xxx
Product-Docker

[Product_Docker_Init]
Product-Docker-Init
[Product_Docker_Init:vars]
ansible_ssh_user=root
ansible_port=22
ansible_ssh_pass={{ secret.root_user_password }}

[product]
Product-Docker-Init
Product-Docker
